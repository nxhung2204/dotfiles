---
name: commit-push
allowed-tools: Bash(git:*), Skill
description: Commit staged changes and push to remote
---

Execute these steps immediately without asking for confirmation:

1. Use /commit skill
2. After that, commit and push to 
3. Run `git push` to push to the remote branch
4. Report the commit hash and push result to the user
